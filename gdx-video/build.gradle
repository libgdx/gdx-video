apply plugin: "java"

sourceSets {
    main {
        java {
            srcDirs = ["src/"]
        }
    }
}

archivesBaseName = 'gdx-video'

dependencies {
    implementation "com.badlogicgames.gdx:gdx:$gdxVersion"
}

eclipse.project {
    name = projectGroup + "-core"
}
